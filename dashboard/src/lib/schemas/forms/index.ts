import { accountSchema, languages } from './account';
import { appearanceSchema, type AppearanceSchema } from './appearance';
import { notificationSchema, type NotificationSchema } from './notification';
import { displaySchema, type DisplaySchema } from './display';
import type { AccountSchema } from './account';

export {
    languages,
    accountSchema, type AccountSchema,
    appearanceSchema, type AppearanceSchema,
    notificationSchema, type NotificationSchema,
    displaySchema, type DisplaySchema
}

